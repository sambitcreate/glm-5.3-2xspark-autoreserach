# Execution reports

Only curated, secret-free reports belong here. Raw host/container manifests, logs, checkpoint paths and run artifacts remain in the ignored `artifacts/` directory.

A report must distinguish GPU smoke checks, synthetic kernel timing, two-rank model inference and a qualified end-to-end performance improvement. A successful compile is not a speedup. No kernel is promoted by the current feasibility probe.
